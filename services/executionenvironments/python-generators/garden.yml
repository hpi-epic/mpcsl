kind: Module
description: Generate a dataset using R
name: mpci-dag-image
dockerfile: Dockerfile
image: ${var.execution-image-namespace}/mpci_dag
extraFlags: ['--tag', '${var.execution-image-namespace}/mpci_dag:latest']
type: container
