apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mpci.fullname" . }}-test-connection"
  labels:
{{ include "mpci.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mpci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
